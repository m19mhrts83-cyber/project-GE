# 運用コマンド一覧（正本）

ターミナルに貼り付けて実行するコマンドの**集約先**です。  
他の `.md` / `.mdc` に同種のコマンドを増やさず、**追記・修正は基本的にこのファイルだけ**にしてください。

## パス（このマシンでの前提）

| 記号 | 意味 |
|------|------|
| `GIT_REPOS` | `~/git-repos` |
| `PY_GMAIL` | `~/git-repos/ProgramCode/venv_gmail/bin/python` |
| `PY_VENV` | `~/git-repos/ProgramCode/venv/bin/python` |
| `YORITOORI_MANUAL` | `~/git-repos/215_kamiooya/C1_cursor/1b_Cursorマニュアル` |
| `ONEDRIVE_215` | `~/Library/CloudStorage/OneDrive-個人用/215_神・大家さん倶楽部` |

- **Python の実行**: Gmail・やり取り系は原則 `PY_GMAIL`。OneDrive 上の `.venv_gmail` は使わない（遅延・不安定回避）。
- **215 のデータ**: スクリプトは `215_kamiooya` 配下の相対パスで `C2_ルーティン作業/26_パートナー社への相談` を参照する。実運用で OneDrive 本体だけを更新している場合は、作業後にリポジトリ側と同期するか、カレントを OneDrive の同じ相対パスに合わせる。

---

## 1. パートナー（やり取り）— Gmail / iMessage / Chatwork / LINE 取り込み

カレントは `YORITOORI_MANUAL`（上表の `~/git-repos/215_kamiooya/C1_cursor/1b_Cursorマニュアル`）。

```bash
cd ~/git-repos/215_kamiooya/C1_cursor/1b_Cursorマニュアル

~/git-repos/ProgramCode/venv_gmail/bin/python gmail_to_yoritoori.py
# 漏れ確認のみ: 末尾に --include-read

~/git-repos/ProgramCode/venv_gmail/bin/python imessage_to_yoritoori.py
```

- **Gmail トークン**: 同一フォルダに `token_estate.json` と `token_m19m.json` がある場合、**環境変数なしで2アカウントを自動処理**（旧 `token.json`+`token2.json` より優先。送信・空室一括 `send_mail.py` / `yoritoori_send.py` は `token.json` がスコープ不足なら `token_estate.json` を自動選択）。作業用の正は OneDrive 側の同フォルダへミラーし、git-repos 側にも同ファイルを置くと上記 `cd` のみで動く。

```bash
# （参考）GMAIL_TOKEN_PATHS を明示するとき
export GMAIL_TOKEN_PATHS="$HOME/git-repos/215_kamiooya/C1_cursor/1b_Cursorマニュアル/token_estate.json,$HOME/git-repos/215_kamiooya/C1_cursor/1b_Cursorマニュアル/token_m19m.json"
cd ~/git-repos/215_kamiooya/C1_cursor/1b_Cursorマニュアル
~/git-repos/ProgramCode/venv_gmail/bin/python gmail_to_yoritoori.py
```

**Chatwork**（`chatwork_to_yoritoori.py` が **git-repos に無い**場合は、OneDrive 側の同フォルダをカレントにする）:

```bash
cd ~/Library/CloudStorage/OneDrive-個人用/215_神・大家さん倶楽部/C1_cursor/1b_Cursorマニュアル
~/git-repos/ProgramCode/venv_gmail/bin/python chatwork_to_yoritoori.py
```

**LINE（CHRLINE sync）**:

```bash
cd ~/git-repos/line_unofficial_poc && .venv/bin/python chrline_sync_to_yoritoori.py --allow-qr-login
```

- プリセット: 既定 `line-default`（**Tcell はグループ「キャラメル管理G」のみ**＋ LEAF グループを同一実行で処理。業務連絡はグループに集約する想定）。Tcell グループのみ `--preset tcell-both`、過去の 1:1 取り込みだけ `--preset tcell-yuki`、LEAF のみ `--preset leaf-grandole`。
- 前提: `LINE_UNOFFICIAL_AUTH_DIR`（**未設定なら** `line_unofficial_poc/.line_auth_local` に自動。クラウド同期パスは非推奨）、必要に応じて `LINE_TCELL_YORITOORI_MD` / `LINE_LEAF_YORITOORI_MD` 等。CHRLINE 再利用時は `savePath/.images/qr_*.png` を実行ルーチンで削除済み。
- セッション失効時は QR 再認証が必要（非公式 API の制約で省略不可）。`line_unofficial_poc/.env` で **`LINE_CHRLINE_AUTO_OPEN_QR=1`** を付けたときだけ QR 画像を自動表示（未設定なら表示せず、ターミナルの `IMG:` パスを手動で開く）。古い保存トークンが残っていれば別ファイルを自動試行して QR 回数を減らす。
- `chrline_sync_to_yoritoori.py` は **既定で QR 再認証を起動しない**（保存トークン無効時は終了）。「やり取り確認」を明示実行するときだけ `--allow-qr-login` を付ける。

**本文取得率の比較検証（defer ON/OFF）**:

```bash
cd ~/git-repos/line_unofficial_poc

# A) 既定（defer ON）
.venv/bin/python chrline_sync_to_yoritoori.py --preset line-default --allow-qr-login --verbose

# B) 比較用（defer OFF）
.venv/bin/python chrline_sync_to_yoritoori.py --preset line-default --allow-qr-login --no-defer-sync-placeholders --verbose

# C) 直近14日で本文取得率を可視化
.venv/bin/python chrline_decode_stats_report.py --since-days 14
```

- `chrline_sync_to_yoritoori.py` は毎回 `LINE_UNOFFICIAL_AUTH_DIR/.chrline_sync_decode_stats.jsonl` に本文取得率を追記（`--decode-stats-file` で変更可）。
- `chrline_decode_stats_report.py` は `defer_sync_placeholders` の ON/OFF 比較と、ターゲット（1:1 / G）別の取得率を表で表示する。

---

## 2. パートナー宛メール送信（yoritoori_send.py）

```bash
cd ~/git-repos/215_kamiooya/C1_cursor/1b_Cursorマニュアル
~/git-repos/ProgramCode/venv_gmail/bin/python yoritoori_send.py --partner ミニテック
# メールと電話の両方がある相手は、ルールに従い --via gmail または --via imessage
```

- 件名・本文: 各パートナーフォルダの `4.送信下書き.txt`
- 連絡先: `215_kamiooya/C2_ルーティン作業/26_パートナー社への相談/000_共通/連絡先一覧.yaml`
- **CHRLINE（LINE 非公式）**: **既定ではセッション確認を行わない**（Gmail 送信中に QR 認証が割り込むのを防ぐため）。必要なときだけ `yoritoori_send.py` に `--check-chrline` を付けて確認する。常に無効化したい場合は `--skip-chrline` または `YORITOORI_SKIP_CHRLINE=1`。

---

## 3. LINE クリップ・エクスポート → やり取り.md

カレントは `YORITOORI_MANUAL`。

```bash
cd ~/git-repos/215_kamiooya/C1_cursor/1b_Cursorマニュアル
~/git-repos/ProgramCode/venv_gmail/bin/python line_to_yoritoori_clip.py --partner Newtus
# 他オプションはスクリプト先頭 docstring 参照
```

---

## 4. LINE オープンチャット差分の蓄積

```bash
cd ~/git-repos/line_unofficial_poc && .venv/bin/python chrline_open_chat_to_md.py
```

- ルート YAML: 環境変数 `LINE_OPEN_CHAT_ROUTES_YAML`（未指定時 `open_chat_routes.yaml`）
- **認証はトーク同期と共有**: `chrline_open_chat_to_md.py` も `chrline_sync_to_yoritoori.py` も同じ `LINE_UNOFFICIAL_AUTH_DIR` と `chrline_client_utils.build_logged_in_client` を使う。**別ディレクトリに分けると QR ログインが冗長に増えやすい**。
- **`.tokens/`**: 複数ファイルを更新日時の新しい順に試行。再認証後は `persist_auth_token` で保存され、次回以降 QR なしで起動しやすい。
- **クラウド同期パス**: `LINE_UNOFFICIAL_AUTH_DIR` が OneDrive 等を含むと警告（セッション競合で QR が増えやすい）。未設定なら `line_unofficial_poc/.line_auth_local` 既定を推奨。
- QR・`LINE_CHRLINE_AUTO_OPEN_QR`: 上記「LINE（CHRLINE sync）」と同じ。セッション失効時の QR 自体は非公式 API の制約で省略不可。
- （任意）疎通監視: `line_unofficial_poc/launchd/open_chat_healthcheck_runner.sh` … 失敗時に通知し再ログインを促す。

### スレッド MID 補助（メインタイムラインから候補抽出・`open_chat_routes.yaml` 追記）

- 参加していないスレッドは `getJoinedSquareChatThreads` に載らないが、**メインタイムラインのイベント**に `threadMid` 等が含まれる場合がある。通常の同期ループでイベントを走査し、候補 MID を集計する。
- **手順**: まず `--dry-run` で追記予定件数を確認し、問題なければ `--dry-run` なしで実行して YAML に追記する。

```bash
cd ~/git-repos/line_unofficial_poc && .venv/bin/python chrline_open_chat_to_md.py \
  --discover-thread-mids --auto-append-thread-mids --dry-run

cd ~/git-repos/line_unofficial_poc && .venv/bin/python chrline_open_chat_to_md.py \
  --discover-thread-mids --auto-append-thread-mids
```

- `--min-hit-count N`: 同一 MID が **N 回以上** 検出されたときだけ `thread_mids` に載せる（ノイズ抑制、既定は 1）。
- `--dry-run`: `5.やり取り.md`・state・dedup に加え、**`open_chat_routes.yaml` も変更しない**。
- 追記後は、次回以降の同期で当該 MID が `thread_mids` 明示ストリームとして取り込まれる（既存の「一覧に無い MID も同期」ロジック）。

---

## 5. 空室対策メール一括送信（interactive_vacancy_send.py）

```bash
cd ~/git-repos/ProgramCode && \
./venv_gmail/bin/python \
  ~/git-repos/215_kamiooya/C1_cursor/mail_automation/interactive_vacancy_send.py
```

- 管理会社一覧 Excel 等のデータパスは Cursor コマンド `vacancy-mail-send` または既存マニュアルに従う。

---

## 6. DX互助会 — いけとも AI ニュース保存・token 連携

スクリプトは `dx_kyouyuu/03_outputs/ai_news_save/`。認証は既定で OneDrive 215 の `1b_Cursorマニュアル` を参照（`gmail_ai_news_save.py` 内のデフォルト）。

```bash
cd ~/git-repos/dx_kyouyuu/03_outputs/ai_news_save
~/git-repos/ProgramCode/venv_gmail/bin/python gmail_ai_news_save.py
# 一覧のみ: --list

~/git-repos/ProgramCode/venv_gmail/bin/python refresh_token_and_update_github_secret.py
```

詳細手順: 同フォルダの `いけともAIニュース保存_セットアップ手順.md`、`GitHub Actions用_token更新手順.md`。

---

## 7. ブラウザ自動化（日能研・東海労金など）

カレントは `~/git-repos/215_kamiooya/C1_cursor/browser_automation`（このフォルダ内 `.venv` を使用）。

```bash
cd ~/git-repos/215_kamiooya/C1_cursor/browser_automation
.venv/bin/python fetch_after_login.py nichinoken
# 東海労金の例は .cursor/commands/tokairokin-transfer.md 参照
.venv/bin/python fetch_after_login.py tokairokin --bank 0005 --branch 405 --account 0526519 --amount 10000
```

---

## 8. Cursor の「コマンドパレット用」定義ファイル

自然言語ワークフローの入口（このリポジトリ内）:

- `~/git-repos/.cursor/commands/*.md`  
  例: `vacancy-mail-send.md`, `mail-draft.md`, `nichinoken-fetch.md`, `tokairokin-transfer.md`

実行時の **Python / venv** はこのドキュメントの表を正とし、`.md` 側のパスが古い場合は **こちらを優先**して更新する。

---

## 9. 関連ルール（方針・トリガー）

| 内容 | 場所 |
|------|------|
| ワークスペース分担（git-repos / OneDrive） | `~/git-repos/.cursor/rules/workspace-structure.mdc` |
| Python は venv_gmail（OneDrive venv 禁止） | `~/git-repos/ProgramCode/.cursor/rules/python-local-venv.mdc` |
| パートナー送信・確認・オープンチャット等 | `215_神・大家さん倶楽部/.cursor/rules/*.mdc`（方針。コマンドは本ファイル） |

---

## 10. 税理士資料（AMEX 明細のダウンロード後整理）

**クレジットカード（AMEX・Vpass）の工程・プログラム一覧・自動化候補の整理**: `ONEDRIVE_215/50_税金,確定申告/knees bee 税理士法人/2.経費,売上資料/税理士提出_手順書/手順_クレジットカード明細_AMEXとVpass_整理.md`

**ファイルの置き場（アウトプットはこのフォルダ配下／サイトから取得した元ファイルも期間フォルダに残す）**: `ONEDRIVE_215/50_税金,確定申告/knees bee 税理士法人/2.経費,売上資料/税理士提出_手順書/README_運用_ファイル保管.md`

**税理士への最終提出（マイコモン／MyKomon・種目別フォルダへ配置）**: ログイン `https://www.mykomon.com/MyKomon/` — 画面: `…/税理士提出_手順書/手順_税理士提出_MyKomonログインと配置.md` — 全体は README の「**税理士提出（マイコモン／MyKomon への配置）**」

**手順書用の画面キャプチャ（PNG・マニュアル貼り付け用）**: `ONEDRIVE_215/50_税金,確定申告/knees bee 税理士法人/2.経費,売上資料/手順書用キャプチャ/README.md`

**PayPay 銀行・口座明細（PDF。命名: `PayPay銀行明細_<月>〜<月>.pdf`、例 `PayPay銀行明細_10月〜4月.pdf`。ダウンロード直後の乱数ファイル名は必ずリネーム）**: `ONEDRIVE_215/50_税金,確定申告/knees bee 税理士法人/2.経費,売上資料/税理士提出_手順書/手順_税理士提出_PayPay銀行明細.md`（ブラウザ手順。スクリプト未整備時は同ファイルのみ参照）

**オリックス銀行・借入金の明細（PDF。命名は `返済実績表_G1_<期間>.pdf`。**終了月は PDF 表の最新月分**に合わせる。照会の「至」が翌月でも行が無ければ `…2026.3月` のように自動でずれる → `orix_repayment_pdf_postprocess.py`）**: `ONEDRIVE_215/50_税金,確定申告/knees bee 税理士法人/2.経費,売上資料/税理士提出_手順書/手順_税理士提出_オリックス銀行借入明細.md`

**ミニテック（スコープ・命名・自動化依頼テンプレの正本。**送金のご案内**は支払年月ごと。**工事完了報告書**列に PDF がある行も取得。詳細画面操作は別ファイル）**: `ONEDRIVE_215/50_税金,確定申告/knees bee 税理士法人/2.経費,売上資料/税理士提出_手順書/手順_MiniTech_税理士提出_まとめ.md` — 操作手順: `…/税理士提出_手順書/手順_税理士提出_ミニテック送金情報照会.md`

**Finto カード後払い（スコープ・差分・命名・自動化テンプレの正本。**振込希望日**・**広告費／修繕費**。操作手順は別ファイル）**: `ONEDRIVE_215/50_税金,確定申告/knees bee 税理士法人/2.経費,売上資料/税理士提出_手順書/手順_Finto後払い_税理士提出_まとめ.md` — 画面詳細: `…/税理士提出_手順書/手順_税理士提出_Finto後払い修繕費明細.md`

**手順書（正本・業務メモ）**: `ONEDRIVE_215/50_税金,確定申告/knees bee 税理士法人/2.経費,売上資料/税理士提出_手順書/手順_税理士提出_AMEX明細.md`

ブラウザで AMEX から CSV／PDF を保存した**あと**、所定フォルダへ移動・リネームする（ログイン操作はしない）。

```bash
cd ~/git-repos/215_kamiooya/C1_cursor/tax_docs_tools

# 例: 2025年7〜9月分を、直近72時間以内に保存されたファイルだけ移動（既定）
python3 amex_statement_postprocess.py \
  --inbox ~/Downloads \
  --materials-root "$HOME/Library/CloudStorage/OneDrive-個人用/215_神・大家さん倶楽部/50_税金,確定申告/knees bee 税理士法人/2.経費,売上資料" \
  --year 2025 --quarter 3

# 事前確認のみ
python3 amex_statement_postprocess.py --inbox ~/Downloads \
  --materials-root "$HOME/Library/CloudStorage/OneDrive-個人用/215_神・大家さん倶楽部/50_税金,確定申告/knees bee 税理士法人/2.経費,売上資料" \
  --year 2025 --quarter 3 --dry-run

# 保存先をフルパスで指定する場合
python3 amex_statement_postprocess.py --inbox ~/Downloads \
  --dest "$HOME/Library/CloudStorage/OneDrive-個人用/215_神・大家さん倶楽部/50_税金,確定申告/knees bee 税理士法人/2.経費,売上資料/2025/7月-9月/07_AMEX明細" \
  --year 2025 --quarter 3 --since-hours 48

# ファイル名に AMEX っぽい文字列があるものだけ（Downloads が混み合っているとき）
python3 amex_statement_postprocess.py --inbox ~/Downloads \
  --materials-root "$HOME/Library/CloudStorage/OneDrive-個人用/215_神・大家さん倶楽部/50_税金,確定申告/knees bee 税理士法人/2.経費,売上資料" \
  --year 2025 --quarter 3 --amex-only
```

- 保存先（`--materials-root` 指定時の既定）: `.../2.経費,売上資料/<年>/<○月-○月>/07_AMEX明細/`
- 取り込み過多を防ぐため、**既定では直近 72 時間以内**に更新されたファイルのみ。古いファイルも含めるときは `--all-inbox`（注意して使用）

**オリックス銀行・返済実績表 PDF（ダウンロード後。表の最新月分に合わせて `返済実績表_G1_….pdf` へコピー）**:

```bash
cd ~/git-repos/215_kamiooya/C1_cursor/tax_docs_tools
pip install -r requirements.txt   # 初回のみ（pypdf）

# 例: Downloads の NBGA….pdf を、所定フォルダへ実データの終了月で命名してコピー
python3 orix_repayment_pdf_postprocess.py \
  --input ~/Downloads/NBGA3160EH8NC0I070HK105345897hADb5NVypWdWhWJ.pdf \
  --dest-dir "$HOME/Library/CloudStorage/OneDrive-個人用/215_神・大家さん倶楽部/50_税金,確定申告/knees bee 税理士法人/2.経費,売上資料/2025/10月-2026.4月/00_元ファイル_サイト取得/オリックス銀行_借入" \
  --group G1

# 事前確認のみ
python3 orix_repayment_pdf_postprocess.py --input ~/Downloads/NBGA3160….pdf \
  --dest-dir "$HOME/Library/CloudStorage/OneDrive-個人用/215_神・大家さん倶楽部/50_税金,確定申告/knees bee 税理士法人/2.経費,売上資料/2025/10月-2026.4月/00_元ファイル_サイト取得/オリックス銀行_借入" \
  --group G1 --dry-run
```

（**ミニテック**は `税理士提出_手順書/手順_MiniTech_税理士提出_まとめ.md` §7 のスコープどおり。ダウンロード後、`00_元ファイル_サイト取得/ミニテック/` へ所定名にリネーム。ブラウザ一括は未整備 → 自動化は同ファイルのコピペ用ブロックを仕様とする。**Finto**は `税理士提出_手順書/手順_Finto後払い_税理士提出_まとめ.md` §8・§9。`00_元ファイル_サイト取得/Finto後払い/`。ブラウザ一括は未整備。）

**費目仕分け（`activity.xlsx` → 税理士提出用に費目列・不要行削除）** — 手順の正本: `ONEDRIVE_215/50_税金,確定申告/knees bee 税理士法人/2.経費,売上資料/税理士提出_手順書/手順_AMEX_費目仕分け_チャット運用.md`

```bash
cd ~/git-repos/215_kamiooya/C1_cursor/tax_docs_tools

# ルールファイル（例: example をコピーして編集）
RULES="$HOME/path/to/amex_himoku_rules.json"

python3 amex_activity_classify.py preview ~/Downloads/activity.xlsx --rules "$RULES"
python3 amex_activity_classify.py preview ~/Downloads/activity.xlsx --rules "$RULES" --csv ~/Desktop/amex_preview.csv

# ルールに当たった行だけ費目を入れ、未マッチは空欄のまま出力（法人経費を少しずつルール化するときの既定）
python3 amex_activity_classify.py finalize ~/Downloads/activity.xlsx --rules "$RULES" \
  --output "$HOME/Library/CloudStorage/OneDrive-個人用/215_神・大家さん倶楽部/50_税金,確定申告/knees bee 税理士法人/2.経費,売上資料/2025/7月-9月/07_AMEX明細/amex_仕分け済.xlsx" \
  --allow-unmapped
# 未マッチを出さない: --drop-unmapped。未マッチでエラー止め: --allow-unmapped なし（手順書参照）

# 費目が入っている行だけ残す（費目ブランクのデータ行を削除）
python3 amex_activity_classify.py export-tagged ~/Downloads/AMEX_4月.xlsx \
  --output ~/Downloads/AMEX_4月.xlsx
```

**Vpass（三井住友カード）明細**: ブラウザ操作・保存先の手順の正本は `ONEDRIVE_215/50_税金,確定申告/knees bee 税理士法人/2.経費,売上資料/税理士提出_手順書/手順_税理士提出_Vpass明細.md`（自動化スコープの説明は `税理士提出_手順書/手順_税理士提出_自動化スコープとAMEX工程.md`）。

**Vpass CSV → 費目付き xlsx（ルールは AMEX と共通 `amex_himoku_rules.json`）** — レイアウトは `vpass_202509.xlsx` 相当（日付・内容・金額・列1・費目）。

```bash
cd ~/git-repos/215_kamiooya/C1_cursor/tax_docs_tools
RULES="$HOME/git-repos/215_kamiooya/C1_cursor/tax_docs_tools/amex_himoku_rules.json"

python3 vpass_csv_classify.py preview ~/Downloads/202604.csv --rules "$RULES"
python3 vpass_csv_classify.py finalize ~/Downloads/202604.csv --rules "$RULES" \
  --output "$HOME/Library/CloudStorage/OneDrive-個人用/215_神・大家さん倶楽部/50_税金,確定申告/knees bee 税理士法人/2.経費,売上資料/2025/10月-2026.4月/vpass_202604.xlsx" \
  --allow-unmapped

# 費目が入っている行だけ（空欄行を削除）
python3 vpass_csv_classify.py export-tagged ~/Desktop/vpass_202604.xlsx --output ~/Desktop/vpass_202604_tagged.xlsx
```
