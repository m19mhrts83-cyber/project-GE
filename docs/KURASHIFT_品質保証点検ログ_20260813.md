# KURASHIFT 品質保証点検ログ（2026-08-13）

**点検者ロール**: 品質保証（コード・ルート煙テスト・仕様突合）  
**宛先**: アプリ実務者（次ロール）  
**本番**: https://jarvis-trade-desk.vercel.app/  
**根拠**: 編集者プラン（Wave 0）／`docs/KURASHIFT_編集者引き継ぎ_不動産AB_20260813.md`／実装コード

---

## 総評（実務者が先に読む）

| 判定 | 内容 |
|---|---|
| **③レーン骨格** | **合格（条件付き）**。A/B/C/D が本番で 200、レーンナビあり、DSCR・合算金利・融資パックの安全境界は仕様どおり |
| **本線「全部満足」** | **まだ宣言しない**。年計画 vs YTD・②ブラウザ通し・案件1件体感・ドキュメント陳腐化が残る |
| **P0（即止め）** | **なし**（今回の点検範囲） |
| **次の実務者の仕事** | 下表「実務者タスク」を上から。実装は Wave1/2、ドキュメント同期は今日中でも可 |

ログイン後の数値目視・スクショは **認証セッションが必要なため未完了**（HTTP 煙＋ロジック検証で代替）。ログインできる環境で **§5 補完チェック**を1周してください。

---

## 1. Wave 0 結果表

| ID | 点検 | 結果 | 根拠・メモ |
|---|---|---|---|
| **QA-0** | 本番 URL 応答 | ✅ 条件付き | `/realestate` ほか 5 URL すべて **HTTP 200**。ログイン後ダッシュボード表示は未確認 |
| **QA-A1** | 名義合算／個人／法人 | ✅ コード | `?scope=` 切替実装あり。切替後の数字変化はログイン後要確認 |
| **QA-A2** | DSCR・RR・返済 | ✅ | A 画面に「簡易＝月家賃÷月返済／NOIではない／目安1.2×」。`simpleDscr` 境界値 OK |
| **QA-A3** | CF50万ギャップ | ✅ コード | A ページにギャップ表示あり（数値はログイン後） |
| **QA-B1** | buy-plan 年表 | ✅ 条件付き | ルート 200・canonical／events 表示実装。events>0 は DB 依存・ログイン後 |
| **QA-B2** | Focus→千三つ | ✅ | buy-plan → deals リンクあり |
| **QA-B3** | deals・送信なし | ✅ | 不動産配下に自動送信・`sendMessage` なし。パックは「自動送信しない」明記 |
| **QA-C1** | properties RR/DSCR | ✅ | DSCR 列あり。**簡易注記は A にあり C に薄い** → P1 |
| **QA-D1** | finance-pack | ✅ | 商品×名義・共通マイナ設計・コピー想定。送信 UI なし |
| **QA-R1** | 合算金利≈2.55% | ✅ 計算 | キャラメル本担保＋コスト: 残高加重 **2.55%**、簡易 DSCR **1.33×** |
| **QA-SAFE** | 未承認実弾なし | ✅ 不動産範囲 | 融資パック文言＋コード grep。①②の live フラグは別途ログイン確認推奨 |

---

## 2. 要件満足マップ（編集者 §4 との突合）

| 要件 | 満足？ | 実務者メモ |
|---|---|---|
| 4レーンナビ分離 | ✅ | ただしナビの short がプラン／千三つとも **「B」**（迷子リスク P2） |
| A: 名義切替 KPI | ✅ 骨格 | 年計画バーなし＝本線ギャップ（Wave2 RE-1b） |
| A: RR・返済・DSCR＋説明 | ✅ | 説明は A のみ明確 |
| B: 長期年表 | ✅ 骨格 | action 生値の読みやすさは編集依頼どおり未磨き（P1） |
| B: 条件→千三つ | ✅ | |
| C: 物件×ローン×RR×合算 | ✅ | as-of 鮮度は提出前に再確認（Wave2 3C-ASOF） |
| D: 商品×名義・マイナ共通・送信しない | ✅ | 状態は **localStorage のみ**（端末依存 P1） |
| ローン二重入力なし | ✅ 設計 | 投影＋sync。実務は `loans.json` 鮮度を見る |
| ①②ブラウザ通し | ❌ 未点検 | V-2-UI |
| 厳密 NOI DSCR | — | **意図的除外**（仕様固定） |

---

## 3. 欠陥・改善（優先度付き）

### P0 — なし

### P1（実務で誤認・手戻りしやすい）

| ID | 内容 | 推奨処置（誰が） |
|---|---|---|
| **DOC-STALE-1** | `docs/KURASHIFT_検証プラン.md` が「③ Phase1〜6 未実装」のまま | 実務者／編集: 「いまの位置」を現行到達に書き換え |
| **DOC-STALE-2** | `docs/KURASHIFT_実務者引き継ぎ.md` が loan-tracker「未使用」・パック「骨格」の古い記述 | 実務者: §2/§3 を本ログと同期 |
| **UX-DSCR-C** | C（properties）に DSCR 簡易注記が薄い | 実装: A と同じ1行をカード／表上に |
| **UX-ACTION** | 年表 `action` が英語／生キーワードのまま読みにくい | 実装／編集: 購入・売却・調達の日本語＋色 |
| **D-LS** | 融資パック進捗がブラウザ localStorage のみ | 実装（後段可）: 案件フォルダ or DB。当面は「端末依存」を UI に強調 |
| **CATALOG-DRIFT** | YAML 正本と `financePackCatalog.ts` が二重管理 | 実装: 生成スクリプト or CI diff。変更時は両方更新 |

### P2（ロバスト／磨き）

| ID | 内容 | 推奨処置 |
|---|---|---|
| **NAV-BB** | レーン短名が B が2つ | 「B計画」「B実行」等に変更 |
| **TEST-UNIT** | `reDscr` / `blendLoanRate` に自動テストなし | vitest 等で境界値を固定 |
| **FOCUS-STATIC** | Notion Focus 要約が静的 | 仕様どおり Excel 正本。鮮度ズレ時は再 ingest |
| **DEPLOY-GIT** | Git→Vercel 自動が不安定で手動 prod 依存 | OPS-DEPLOY |
| **SHOTS** | A KPI／B 年表のスクショが docs 未 | ログイン後に1枚ずつ |

### ロバストさ（良い点）

- `simpleDscr`: 返済≤0・家賃負・null → `null`（ゼロ除算なし）
- `blendLoanRate`: 残高／金利欠落を除外し件数を返す
- 融資パック: 対外送信なしを UI で明示
- レーン分離: 5ページすべて `RealEstateLaneNav`

---

## 4. 実務者タスク（作業しやすい順）

### 今日〜明日（ドキュメント／確認・コード少）

1. **ログイン補完（§5）** — 本番で A/B/C/D を目視。合否を本ログ末尾に追記してよい  
2. **DOC-STALE-1/2** — 検証プラン「いまの位置」＋実務者引き継ぎの DONE を現行化  
3. **キャラメル確認** — C で合算金利≈2.55%、DSCR≈1.33×、本担保＋コスト2本が見えるか  
4. **融資パック通し** — 物件購入×個人で不足コピー→メール下書きコピーのみ（送信しない）

### 次スプリント（実装ロールへ依頼）

5. **P1**: UX-DSCR-C / UX-ACTION /（任意）D-LS 文言強化  
6. **Wave2**: RE-1b（年計画 vs YTD）→ V-2-UI → 案件 draft→内見 1件  
7. **3C-ASOF** — 提出前に loan-tracker as-of 再 sync  
8. **OPS-DEPLOY** — 自動デプロイ安定化

### やらない（本線と混ぜない）

- 厳密 NOI DSCR、自動問い合わせ送信、健美家 CRM、Lab立花、B-IFA 返信待ち

---

## 5. ログイン後の補完チェック（実務者用コピペ）

```
[ ] QA-0 ログイン後ホーム表示
[ ] A scope=combined / personal / corporate で KPI が変わる
[ ] A DSCR 注記が読める・3物件に数字
[ ] B buy-plan: canonical 版名・events > 0・年表ブロック
[ ] B Focus 条件が戸建/20%/300万/土地値60%/愛知と矛盾しない
[ ] B → 千三つリンク到達・送信ボタンなし
[ ] C キャラメル: RR合計・月返済・家賃−返済・合算≈2.55%
[ ] D マイナが共通に出る・不足リストコピー可・送信なし
[ ] /lifeplan /tax /roi が開ける（V-2-UI の入口）
```

コマンド（ローカル検証）:

```bash
cd ~/git-repos/apps/trade-desk && npx tsc --noEmit
# ローン投影更新（必要なとき）
cd ~/git-repos && set -a && source .env.jarvis_private && set +a
~/selenium_env/venv/bin/python scripts/jarvis_kurashift_loan_tracker_sync.py --apply
```

---

## 6. 参照リンク

| 文書 | 用途 |
|---|---|
| `docs/KURASHIFT_品質保証点検_実行プラン_20260813.md` | Wave 定義・満足の定義 |
| `docs/KURASHIFT_編集者引き継ぎ_不動産AB_20260813.md` | 開発→編集の意図 |
| `docs/KURASHIFT_実務者引き継ぎ.md` | 再開入口（要更新） |
| `docs/KURASHIFT_loan_tracker_Discover.md` §B-RATE-4 | 合算金利式 |
| `docs/KURASHIFT_融資提出パック.md` | D 仕様 |

---

## 7. 点検メタ

| 項目 | 値 |
|---|---|
| 日時 | 2026-08-13 |
| 手法 | 本番 HTTP 煙／静的コードレビュー／境界値計算／編集プラン突合 |
| 未実施 | 認証付き UI 目視・スクショ・Zaim live フラグの画面確認 |
| 署名 | Jarvis（品質保証点検ロール） |

---

## 8. 実務者実装追記（2026-08-13）

| ID | 処置 | 結果 |
|---|---|---|
| UX-DSCR-C | C に簡易 DSCR 注記 | ✅ |
| UX-ACTION | `buyPlanAction.ts`＋年表バッジ色分け | ✅ |
| D-LS | 端末依存を強調表示 | ✅ |
| NAV-BB | ナビ short を B計／B実 | ✅ |
| RE-1b | A に年計画 vs YTD バー | ✅（合算=目標50万×月、個人法人=満室想定） |
| CATALOG-DRIFT | `scripts/jarvis_kurashift_finance_catalog_diff.py` | ✅ 検知スクリプト |

未: ログイン目視補完・V-2-UI・案件1件・厳密 Numbers 計画行の接続（現状は目標／満室想定プロキシ）

