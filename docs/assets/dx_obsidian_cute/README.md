# dx_obsidian_cute

`Obsidian紹介_DX互助会向け_cute.html` 用アセット。

**方針更新（2026）:** 出来栄え優先の場合は **NotebookLM（Nano Banana / Pro 等）で生成した PNG** を `sN.png` として置き、HTML の `<img src>` を **`.png` に差し替える**。以下の SVG は暫定・フォールバック用。

現状の **手描き風 SVG**（白背景・細い線・淡色の塗り）:

## 差し替え

- **PNG にしたい場合**: 同じファイル名で `s2.png` 等を置き、HTML の `src` を `.png` に変更するか、`.svg` を上書き export する。
- **画像生成 AI を使う場合**: `docs/_drafts/slides_outline_obsidian_cute.md` の「図案」列をプロンプトにし、出力をここに保存。

## ファイル

| ファイル | スライド |
|----------|----------|
| s1_title.svg | タイトル |
| s2.svg … s11.svg | s2〜s11（s3 は表のみのためイラストファイルなし） |
