# Todoist タスク正本 — 設計（2026-09-21）

Cursor plan: `~/.cursor/plans/todoist全レーン試験導入_865b6588.plan.md`

## 方針（確定）

- タスク正本: **Todoist（Beginner 起点）**
- **操作者分離（2026-09-21）**: Mac／CLI／inbox apply は **Jarvis 分身**（`JARVIS_TODOIST_EMAIL`）。松野は admin で UI のみ。コメント見た目＝Jarvis アイコン
- Notion: タスク看板以外は維持（purchase_pre・ナレッジ等）
- 課金: Beginner → 触感確認 → Pro 試し → 良ければ**月額**
- 構成: Beginner は圧縮プロジェクト → Pro で 1レーン1プロジェクト正規化
- 抽出: 「タスク」自然語＋相手の要確認フレーズは**提案型**。記号語尾は任意
- 今フェーズ: **アプリ未確認残を Todoist に載せて処置を回す**

## ファイル

| 役割 | パス |
|---|---|
| プロジェクト／レーン | `config/todoist_projects.yaml` |
| 抽出シグナル | `config/todoist_extract_signals.yaml` |
| CLI | `scripts/jarvis_todoist_api.py` |
| 歪みログ例 | `.jarvis_state/todoist_beginner_distortions.example.json` |
| 秘密 | `.env.jarvis_private` → `TODOIST_API_TOKEN`（**Jarvis 分身**本線）／`TODOIST_API_TOKEN_OWNER`（admin 退避・常用しない）／`JARVIS_TODOIST_EMAIL` |

## 初回手順（admin Beginner）

1. [Todoist](https://todoist.com/) に **admin メール**で新規登録（旧 m19m は使わない）
2. Settings → Integrations → Developer → **API token** をコピー
3. `.env.jarvis_private` に追記（チャットに貼らない）:
   `TODOIST_API_TOKEN=...`
4. 保存後「保存した」と Jarvis へ一声
5. Jarvis が実行:

```bash
cd ~/git-repos && set -a && source .env.jarvis_private && set +a
~/selenium_env/venv/bin/python scripts/jarvis_todoist_api.py whoami
~/selenium_env/venv/bin/python scripts/jarvis_todoist_api.py bootstrap --write-config
~/selenium_env/venv/bin/python scripts/jarvis_todoist_api.py seed-nokori --dry-run
~/selenium_env/venv/bin/python scripts/jarvis_todoist_api.py seed-nokori --apply
```

6. アプリで Board レイアウトを確認し、歪みがあれば distortions JSON に追記

## Beginner 圧縮（5プロジェクト）

| プロジェクト | 内容 |
|---|---|
| 所有物件 | properties（**物件×管理会社はラベル**。セクションは進捗） |
| 業務まとめ | ai_raimo / kamiooya / kanji（ラベル同居） |
| **アプリ開発** | apps（2026-09-21・5本目で専用化） |
| 戸建て | kodate |
| 家族 | kazoku |

セクション列: 未着手 / 進行中 / 相手待ち / **HOLD** / オーナー確認

| 列 | 意味 |
|---|---|
| HOLD | 一時保留（再開待ち）。終わった扱いにはしない。移設時 `HOLD since` 印＋due+30。**1ヶ月超は月次レビュー**（`jarvis_todoist_hold_review.py`）。UI移動の穴埋めは **GHA `todoist-hold-stamp.yml`** ＋朝オープン soft-fail |
| オーナー確認 | 完了候補。松野了承後に `complete-task`（チェック完了） |
| （なし） | **「完了」列は使わない**（2026-09-21）。駐車用に残すと未 close のまま残る） |

※ 個人プロジェクト枠は Beginner 上限付近。これ以上の分割は Pro 正規化（1レーン1プロジェクト）で行う。

## 所有物件の整理（2026-09-21 確定）

**ハッシュタグだけではない。** 切り方は次のとおり。

| 部品 | 用途 |
|---|---|
| プロジェクト `#所有物件` | 実行キューの箱（1本） |
| セクション（Board列） | **進捗**（未着手〜HOLD〜オーナー確認）。物件名にはしない。完了はチェック |
| ラベル `@GrandoleI` 等 | **物件** |
| ラベル `@LEAF` 等 | **管理会社（主担当PM）** |
| タイトル先頭 | `[GrandoleI][ミニテック] 202号 …`（目視用） |

### 物件ラベル（2026-09-21 訂正）

| ラベル | 表示名 | 管理会社 |
|---|---|---|
| `GrandoleI` | Grandole志賀本通I | **主**ミニテック。LEAF・Tcell も関与可 |
| `GrandoleII` | Grandole志賀本通II | ホームプランナーのみ |
| `キャラメル` | キャラメル | Tcellのみ |

※ **`LEAF京都` は物件ではない**（京都銀行の呼称）。物件ラベルに使わない。`LEAF` は管理会社ラベルのみ。

### 管理会社ラベル

`ミニテック` / `ホームプランナー` / `Tcell` / `LEAF`

- 号室はラベルにしない（タイトルへ）
- PMラベルは **いまボールの社**（GrandoleI で LEAF 対応なら `@GrandoleI` `@LEAF`）
- 見方（Beginner）: 検索 `@GrandoleI` またはラベル一覧をクリック。物件専用 Filter は **Pro 以降**
- 一覧正本: `.jarvis_state/todoist_property_labels.json` · YAML: `property_labels` / `pm_labels`

### Guest との関係

- **内部**: 上のラベルで全物件を `所有物件` で追う
- **外部共有（Phase4）**: 将来の `PM_*` は相手に見せてよいタスクだけの別プロジェクト。`所有物件` 全体は共有しない
- **再開条件**: Beginner プロジェクト枠の空き、または Pro。枠満杯のため当面は **要約フッター**（`scripts/jarvis_todoist_pm_status_footer.py`）
- **転送メール**: 相手に渡さない（松野専用）。評価: `docs/Todoist_Guest共有_管理会社向け_20260921.md`

起票例:

```bash
~/selenium_env/venv/bin/python scripts/jarvis_todoist_api.py create-task \
  --lane properties \
  --title '[GrandoleI][ミニテック] 202号 水漏れ手配' \
  --label GrandoleI,ミニテック
```

※ `--lane properties` は **物件ラベル1＋PMラベル1必須**（不足時は create-task がエラー）。

### 朝レビュー順（2026-09-21）

`要連携 → SecondBrain → パッと → GenMail要対応(提案) → オーナー確認 → その他未着手`

- GenMail: admin 接続（m19m/estate 集約）。アプリ Super Agent の **Needs You** を確認 → 入口 `.jarvis_state/genmail_action_needed.md` → `scripts/jarvis_todoist_genmail_propose.py`（了承後 `--apply`）。公開 API なし
- ダッシュボード triage は別系統（本節の「GenMail要対応」と混同しない）

## ダッシュボード投影（Phase 2）

- 切替: `JARVIS_TASK_BACKEND=todoist`（戻しは `notion`）
- 実装: `apps/jarvis-dashboard/lib/taskBoard.ts` / `todoistTasks.ts`
- トークン投影: `scripts/jarvis_todoist_token_sync.py`（dashboard `.env.local` + Vercel）
- UX: 本線は Todoist Board。ダッシュボードは参考 Kanban＋処置カードと並べて見る（DnD 可）

## 完了・起票の役割（2026-09-21）

ルール正本: `.cursor/rules/jarvis-todoist-owner-confirm.mdc`

| 誰 | 起票 | 完了 |
|---|---|---|
| Jarvis | 会話中の1件 | **オーナー確認 → 了承後 close**（勝手に完了しない） |
| FRIDAY | 実装に紐づく件 | 同上 |
| ホーク | **週次・横断・要ボスは優先** | 候補はオーナー確認。了承後のみ done |
| 不動産部長 | 物件レーンの決定事項 | 同上（inbox 経由） |

### API 共有（ホーク／部長）— 検討結果

| 案 | 採否 | 理由 |
|---|---|---|
| Drive inbox `action: todoist_tasks` → Jarvis apply | **本線（採用確定 2026-09-21）** | 秘密を Grok に渡さない。Notion と同型 |
| プロジェクト転送メール | 補助 | 起票のみ向き |
| `TODOIST_API_TOKEN` を Bot に共有 | **見送り** | 漏洩・ログ再掲・失効時の波及が大きい |

適用スクリプト: `scripts/jarvis_hawk_todoist_tasks_apply.py`（`--dry-run` / `--apply` / `--archive-done`）。  
`summary` / `links` は Todoist コメントへ転記（サマリ・アウトプット追跡用）。移行期は `notion_tasks` も可。

### 懸念事項

1. **二重起票**（Jarvis とホークが同件を別名で作成）→ 下記「二重起票防止」で照合
2. **オーナー確認の滞留** → 週次でホークがオーナー確認列をレビュー
3. **Grok に API を渡すと** Instructions／チャット／ログに秘密が載りうる → inbox 本線を先に回す
4. **Beginner の履歴1週間** → 完了コメントの証跡が薄い。重要な了承はチャットにも残す
5. **FRIDAY の Todoist** → **Jarvis 同等**（`scripts/friday_todoist.sh`）。`.env` は cat せずラッパー経由。完了はオーナー確認二段（代理専用フローは不要）

## 二重起票防止（タイトル／L-id 突合・2026-09-21）

**原則**: 同じ仕事を Jarvis とホーク（または部長）が別名で2つ作らない。疑わしいときは **新規 create せず、既存の更新・列移動・コメント追記**。

### 起票前チェック（必須・30秒）

1. **L-id**（例: `L-01` / `T-03`）がタイトルや説明にあれば、同レーンでその文字列を検索  
   → ヒットしたら **そのタスクを使う**（完了済みならコメントで再オープン要否を聞く）
2. **タイトルの核**（物件名＋号室／相手名＋用件／機能名）で同レーンを検索  
   → 似ていれば既存にコメント「同一件として扱う」＋必要なら列移動のみ
3. 見つからなければ create。タイトル先頭に既存の記号規約を維持（`[L-xx]` / `[apps]` 等）

### 役割ごとの約束

| 誰 | やる | やらない |
|---|---|---|
| **ホーク／部長**（inbox `todoist_tasks`） | create 前に同タイトル／L-id を意識。迷ったら `comment` または `owner_confirm` のみ | 毎週同じ文言で新規を量産 |
| **Jarvis**（会話中） | 話題の1件は既存を探してから create。ホーク起票済みなら列だけ直す | ホークと同趣旨の別タイトル新規 |
| **松野** | 二重に見えたらコメントで片方を正と宣言 | — |

### 見つかったときの処置

| 状況 | 処置 |
|---|---|
| 同 L-id・別タイトル | 片方に統合コメント → 不要側は完了（「二重のため閉じる」） |
| 同趣旨・L-id なし | 新しい方を閉じるか、古い方へリネームして一本化 |
| 本当に別件 | タイトルで差が分かるようにして両方残す |

自動化（タイトル類似度スコア等）は後続。当面は **運用＋検索** で足りる。

## コメント運用（2026-09-21・必須）

- 全文入力はしない。タスク紐づきの **処置・確認メモ** 用。
- **サマリ＋アウトプット**をコメントに残す。アウトプットは **Markdown ハイパーリンク** `[表示](https://…)`（`scripts/jarvis_todoist_comment_links.py`）。
  - https／ダッシュボードはそのまま
  - リポ相対パスは **`origin/main` に実在するときだけ** GitHub リンク（未pushはパス表記のみ＝404回避）
- 連携ラベル: `要Jarvis連携` / `要ボス` / `要ホーク` / `quick`
- Jarvis → Drive outbox は条件付き。空の Jarvisボックスは正常。ホーク判断は `要ホーク`
- inbox `action: todoist_tasks` は `jarvis_bucho_inbox_poll` が自動 apply（Mac起動直後〜最長15分）
- Jarvis 参照先: `.cursor/rules/jarvis-reference-sources.mdc`
- **コメント入口**: **本線＝jarvis@ 未読通知 → 対応後既読**／**保険＝API 巡回**（`scripts/jarvis_todoist_comment_inbox.py`）

## Filter・朝レビュー（Todoist UI・手作業）

Beginner の Filter 枠は **3つ**。2026-09-21 入替: **要ホーク枠 → SecondBrain**（会議起票の見渡しを優先。要ホーク専用 Filter は Pro 以降）。

| Filter名 | クエリ（目安） | 用途 |
|---|---|---|
| **要連携** | `@要Jarvis連携 \| @要ボス` | 朝いち。Jarvis／自分の判断 |
| **SecondBrain** | `@SecondBrain` | SB/Genspark から起票した未整理（`jarvis_todoist_sb_propose` が付与） |
| **パッと** | `@quick` | 5分以内で潰す |

（Todoistのラベル記法は環境により `@ラベル` または `label:ラベル`。UIのラベル選択で作ってよい。）

**朝レビュー順（確定）**: 要連携 → SecondBrain → パッと → オーナー確認セクション → その他未着手

**Pro 以降で戻す／増やす候補**: `@要ホーク` 専用 Filter、管理会社別、相手待ち横断など。

## 次フェーズ（未実装／進行）

- ~~`action: todoist_tasks` 適用スクリプト~~ → **実装済**（自動 apply 含む）
- ~~サマリ／リンクコメント~~ → **必須＋ハイパーリンク（origin/main のみ GitHub）**
- ~~フォーク→Todoist ラベル／paste~~ → **反映済**（参謀 paste・Filter案内。B1 は shared_working へ再生成案内）
- ~~Todoist コメント入口（メール本線＋API保険）~~ → **実装済**（`jarvis_todoist_comment_inbox.py`）
- ~~SB/Genspark → 候補提案（Phase 3b）~~ → **実装済**（`jarvis_todoist_sb_propose.py`。了承後 `--apply`）
- Guest パイロット（LEAF＋ホームプランナー）— **枠 or Pro 後**。当面は要約フッター。転送メールは相手非公開
- GenMail 要対応 → Todoist 提案（`jarvis_todoist_genmail_propose.py`。admin）
- Pro Calendar layout 試行（予定正本は admin Googleカレンダー）

### SB/Genspark → Todoist（Phase 3b）

| 手順 | コマンド |
|---|---|
| 提案 | `scripts/jarvis_todoist_sb_propose.py --latest`（または `--meeting-id` / `--keyword`） |
| 了承後 | 同スクリプト `--apply --select 1`（自分分のみなら `--mine-only`） |
| 状態 | `.jarvis_state/todoist_sb_propose.json` |

抽出: Genspark summary の **Action Items**（L2）＋ `todoist_extract_signals.yaml` の self/partner（L1/L3）。**L5 サイレント起票はしない。**
