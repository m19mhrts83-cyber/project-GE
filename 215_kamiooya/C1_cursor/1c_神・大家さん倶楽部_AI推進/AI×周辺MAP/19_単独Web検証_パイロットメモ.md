# 単独Web検証 — パイロットメモ（Phase 5）

- **作成日**: 2026-08-01
- **対象**: 神・大家メンバー 2〜3名
- **完了定義**: 色付き下地（C0）まで。C1は任意
- **入口**: `shuhen-auto.html`（Raimo用語なし）

関連: [`18_単独Web検証_仕様.md`](18_単独Web検証_仕様.md)

---

## 起動（検証ホスト）

```bash
cd ~/git-repos
set -a && source .env.jarvis_private && set +a
# 任意: 共有リンク用トークン / ポート
# export SHUHEN_AUTO_TOKEN='短い共有用文字列'
# export SHUHEN_AUTO_PORT=8770
/Users/matsunomasaharu2/selenium_env/venv/bin/python scripts/shuhen_auto_server.py
```

メンバーURL例:

- ローカル: `http://127.0.0.1:8770/shuhen-auto.html`（`./scripts/shuhen_auto_open.sh`）
- キャラメル試走: `./scripts/shuhen_auto_open.sh --caramel`
- トークンあり: `...?token=...`
- Pages静的UIのみの場合は `?api=https://（APIホスト）` が必要（生成APIは Pages 単体では動かない）
- ※ 8765 は Jarvis ダッシュボード。周辺MAP API は **8770**

**検証済み（2026-08-01・Grandole）**: CLI で施設8/8 Places確定・C0 PNG生成成功。C1は `gemini-2.5-flash-image` で ready。  
**自分用試走（2026-08-01・キャラメル）**: ローカル `:8770` で C0 まで生成。簡略版としては「わりかし簡単で十分」との所感。こだわりを見せる仕上げはなお難しい、とのコメントあり。

CLIスモーク:

```bash
cd ~/git-repos && set -a && source .env.jarvis_private && set +a
/Users/matsunomasaharu2/selenium_env/venv/bin/python scripts/shuhen_auto_pipeline.py \
  --property Grandole志賀本通 \
  --address '愛知県名古屋市北区杉栄町' \
  --out /tmp/shuhen_grandole
```

---

## 計測シート（コピーして使う）

| # | 日付 | 参加者 | 物件 | 所要分 | 施設件数 | 明らかな誤り | C1利用 | C1採用 | 完了(C0まで) | メモ |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 |  |  |  |  |  |  | 有/無 | 有/無/— | 有/無 |  |
| 2 |  |  |  |  |  |  |  |  |  |  |
| 3 |  |  |  |  |  |  |  |  |  |  |

**成功の目安**: 3件中2件以上が「完了(C0まで)=有」、明らかな誤りが致命的でないこと。

---

## メンバー向け一言案内（貼付用）

```text
周辺MAPを自動作成する検証ページです。
1. 物件名と住所を入れる
2. 「周辺MAPを作成」を押す（数分かかることがあります）
3. 周辺施設・地図・ベージュの色付き下地が出ればOK
4. 「色味をさらに寄せる」は任意。道路がおかしいときは破棄
印刷用の最終仕上げはこれまでどおり Canva でお願いします。
```

---

## フィードバック記録

### 2026-08-01 — 自分用試走（キャラメル／緑区文久山）

| 項目 | 内容 |
|---|---|
| 所感 | 簡略版としてはこのまま十分・わりかし簡単にできる |
| こだわりの所在 | Canva／C1 だけでなく、**選んだ周辺施設がどこまで正しいか**。ChatGPT／Gemini＋プロンプトでは「概要→検証→正しいものを選んだか」が見えるが、現状アプリではその過程が分からない |
| 理想像 | 従来の Deep Research 等の調べ結果をアプリ上に出し、それを見ながら**見直し・差し替え**できること |
| 完了 | C0 まで到達（ローカル API） |

### 回収要望（実装バックログ）

優先度はパイロット後に再判定。実装時は本表を正とする。

| # | 要望 | 背景・意図 | 実装メモ（たたき） | 状態 |
|---|---|---|---|---|
| R1 | **保存サイズを A4 横前提に** | 周辺MAPは基本 A4 横（297×210mm）で作る。いまの保存サイズだと後工程と合わない | C0/Static の出力を横長 A4 比率に。目安 **3508×2480 px（300dpi）** または同等比率。`13`/`15` と揃える | **対応済**（2026-08-01） |
| R2 | **掲載施設は徒歩圏を優先（遠すぎない）** | 遠すぎると魅力が伝わらない。今回（キャラメル）は距離が長い印象 | 優先: **徒歩〜7分／〜20分**。上限目安 **徒歩30分圏内**。30分超は原則除外 or 優先度を落とす。地図ズームも近い施設にフィット。既存 Step1「徒歩約10分優先／15分超はB以下」とも整合 | **対応済**（2026-08-01） |
| R3 | **調査・検証の過程をアプリに出す（見直し可能に）** | … | … | **対応済（段階1+2+3）**（2026-08-01）。段階3=アプリ内 Deep Research 相当（検索グラウンディング）＋反映／Step1.2渡す用コピー |

---
